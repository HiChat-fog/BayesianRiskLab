numpy>=1.24
scipy>=1.10
pandas>=2.0
pymc>=5.0
arviz>=0.15
matplotlib>=3.7
# pyDOE2  # optional: for LHS sampling, install separately if needed
# nutpie  # optional: faster NUTS sampler, install separately if available
pytest>=7.0
